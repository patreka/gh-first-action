name: multi line command
on: workflow_dispatch
jobs:
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: multi line command
        run:  |
              echo "First output"
              echo  "Swcond outputs"
              echo "Done multi line command"
